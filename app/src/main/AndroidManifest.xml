<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nur_hidayat_agung.kalbeapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.sales.SalesMenuActivity"></activity>
        <activity android:name=".ui.product.ProductCustomActivity" />
        <activity android:name=".ui.customer.CustomCustomer" />
        <activity android:name=".ui.brand.BrandActivity" />
        <activity android:name=".ui.sales.SellingActivity" />
        <activity android:name=".ui.customer.CustomerActivity" />
        <activity android:name=".ui.product.ProductActivity" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>